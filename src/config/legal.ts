/**
 * Legal Configuration
 *
 * Single source of truth for legal version constants used across
 * GDPR compliance features (terms audit trail, re-consent checks).
 *
 * TERMS_VERSION must match LEGAL_LAST_UPDATED.termsOfUse on the
 * frontend (currently defined in messages/en.json).
 */
export const CURRENT_TERMS_VERSION = '2025-01-15';
